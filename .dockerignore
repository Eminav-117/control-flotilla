node_modules
dist
coverage
.git
.github
.vscode
.claude
.gemini
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store
# NOTA: docs (README, ROADMAP, BRIEFING) NO se excluyen.
# Runtime image es nginx + dist/ only (multi-stage), así que no impactan size final.
# Incluirlos ayuda a debug durante `docker exec` en stage build si algo falla en CI.
