{
  "name": "@control-flotilla/backend",
  "version": "0.1.0",
  "private": true,
  "description": "Lambda handlers for control-flotilla API (DynamoDB single-table + S3)",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.654.0",
    "@aws-sdk/client-s3": "^3.654.0",
    "@aws-sdk/lib-dynamodb": "^3.654.0",
    "@aws-sdk/s3-request-presigner": "^3.654.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.143",
    "@types/node": "^20.11.0",
    "aws-sdk-client-mock": "^4.1.0",
    "typescript": "~5.3.0",
    "vitest": "^4.1.4"
  }
}
