node_modules
dist
.vite
coverage
*.log
.env
.env.*
!.env.example
.DS_Store
Thumbs.db
temp_semanal/
*.backup.html
Control vehicular Semanal ROF-export.zip
Seguimiento de unidades en el taller.xlsx

# public/ ahora solo contiene assets PWA reales (favicon.svg) — SÍ commiteados.
# Fixtures de tests movidos a tests/fixtures/ (binarios grandes, no se commiten).
tests/fixtures/
_legacy/
.husky/_

# Claude Code / IDE locales (launch.json sí se commitea)
.claude/settings.local.json
.vscode/
.idea/
*.swp

# Build extra
dev-dist/
tsconfig.tsbuildinfo

# TypeScript build output (tsc -b emite .js junto a .ts; usamos tsc --noEmit ahora)
src/**/*.js
src/**/*.js.map
src/**/*.d.ts
tests/**/*.js
tests/**/*.js.map

# Playwright
playwright-report/
test-results/
